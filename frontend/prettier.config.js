/** @type {import('prettier').Options} */
module.exports = {
  arrowParens: 'avoid',
  printWidth: 80,
  singleQuote: true,
  semi: false,
  trailingComma: 'all',
  plugins: ['prettier-plugin-tailwindcss'],
}
